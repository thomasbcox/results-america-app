# Database Configuration
# For development: Use local PostgreSQL or cloud database
DATABASE_URL="postgresql://username:password@localhost:5432/results_america_dev"

# For production: Use Vercel PostgreSQL or other cloud database
# DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"

# Optional: Separate development database URL
# DEV_DATABASE_URL="postgresql://username:password@localhost:5432/results_america_dev"

# Admin Recovery Token (for emergency admin creation)
ADMIN_RECOVERY_TOKEN="your-secret-recovery-token"

# Email Configuration (for magic links)
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-app-password"

# Resend Email Service (for magic links)
RESEND_API_KEY="re_77LyBWeN_Nk9Mz9Dk3oKPfJ73jMo3EaRB"
EMAIL_FROM="thomas@eudae.biz"

# Next.js Configuration
NEXTAUTH_SECRET="your-nextauth-secret"
NEXTAUTH_URL="http://localhost:3050"

# App URLs
# For development
NEXT_PUBLIC_APP_URL="http://localhost:3050"

# For production (set in Vercel environment variables)
# NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# Note: NEXT_PUBLIC_API_URL is only needed if you have a separate API server
# Since we use Next.js API routes, the API is part of the same app

# Bootstrap Security (Production Only)
# Generate a secure random token: openssl rand -base64 32
# BOOTSTRAP_SECRET="your-super-secure-random-token-here"

# Optional: Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED="1" 